### Added
- `lium provider node status <id> [--watch]` (also `lium mine status <id>`): which validator
  step the node is on, elapsed and estimated time left while a check runs, and the last run's
  per-step timeline with ✓/✗ and durations (portal `GET /executors/{id}/verification`, DAH-3019).
