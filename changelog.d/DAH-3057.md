### Added
- SDK exceptions carry the API's `code`, `hint` and `request_id` (from the error envelope or the `X-Request-Id` header); the CLI prints the hint and `request_id: …` under every API error and puts `code`/`request_id` in the `--json` envelope, so a failure can be quoted to support and keyed on by agents.
