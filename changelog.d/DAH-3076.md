### Fixed
- `lium mine` from the Linux binary no longer breaks `apt-get` and the other tools it runs: children get the host's `LD_LIBRARY_PATH` instead of the bundle's; a failed step prints the tail of its output and exits 1, naming the step and the reason, instead of ending with a green summary.
