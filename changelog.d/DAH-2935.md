### Fixed
- `lium provider status` and `lium provider node list` now scope the portal's global `GET /executors` listing to the active hotkey, so a provider sees only its own nodes (previously other providers' machines were reported as the caller's). `node list --all` returns the unfiltered listing; `--miner-hotkey` still targets one provider. (DAH-2935)
