### Fixed
- `lium mine`: leaving "Public SSH port" blank now writes `SSH_PUBLIC_PORT=<SSH_PORT>` instead of keeping the template's `2200`, so the validator is told the port sshd actually listens on; a failed `docker compose up` shows the last 4,000 characters of its output (the error line), not the first (pull progress).
