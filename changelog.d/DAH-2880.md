### Added
- Docs: `docs/agents.md`, a single page an LLM agent or unattended script follows end to end (environment-variable auth, JSON output and the error envelope, exit codes, rent → exec → GPU stats → rsync → rm, pod gotchas)). Every command on the page is checked against the click command tree by `test/test_agent_docs.py`, so the page cannot describe a flag this CLI does not have. Linked from the README.
