### Added
- `lium audit [--pod] [--since] [--key] [--json]`: the account's event log — every rent, reboot, edit and delete with the session or API key that requested it (`GET /users/me/events`, lium-platform DAH-2934). Answers for deleted pods by full id.
- SDK: `Lium.events(since=, pod_id=, api_key_id=, limit=)`.
