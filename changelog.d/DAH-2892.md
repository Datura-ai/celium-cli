- `lium templates` shows a `CUDA` and a `Runs on` column (Hopper, Blackwell / Hopper only / pre-Hopper) derived from the CUDA build in the image tag; `--arch hopper|blackwell` keeps only templates known to run on that generation (Blackwell needs CUDA 12.8+). `--format json` rows gain `cuda_version`, `torch_version` and `arch`.
