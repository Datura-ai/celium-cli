### Changed
- `lium ls` default order is cheapest `$/GPU·h` first (was: ★ Pareto nodes first, then fastest download). The ★ still marks Pareto-optimal nodes where they land; `--sort` overrides. `lium up <index>` is numbered from the same order `ls` prints.
