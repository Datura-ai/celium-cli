### Fixed
- `lium ps` (table and `--format json`) and `lium describe` showed the whole host for a GPU-split rental (1 GPU of a 3×RTX 3090 node read `3×RTX3090`, per-GPU price divided by 3); the Config column, JSON `gpu_count`/`config`, `describe`'s `gpu.count` and `executor.price_per_gpu` now follow the pod's own `PodInfo.gpu_count` (DAH-2877); `PodInfo.executor.gpu_count` stays the host's count.
