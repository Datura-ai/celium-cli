### Fixed
- `lium ps` (table and `--format json`) and `PodInfo.executor` showed the whole host for a GPU-split rental (1 GPU of a 3×RTX 3090 node read `3×RTX3090`, per-GPU price divided by 3); the pod's own `gpu_count` is now used.
