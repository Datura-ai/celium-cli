### Changed
- `lium ls` Disk column is now the host's **free** disk (`Disk free (Gb)`), which is what a pod's allocation is computed from; `--format json` keeps `disk_gb` (now free) and adds `disk_total_gb`. The total was shown before and no pod ever gets it (524 GB listed → 130 GB `/workspace` measured).
