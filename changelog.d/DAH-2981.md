### Added
- `lium ls` shows a **CPUs** column (`specs.cpu.count`) and `cpu_count` in `--format json`; `--min-cpus N` on `lium ls` and `lium up` keeps only nodes that report at least N CPU threads. SDK: `ExecutorInfo.cpu_count`, `Lium.ls(min_cpus=)`. (DAH-2981)
