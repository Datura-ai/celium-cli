### Security
- The Linux binaries bundle OpenSSL `1.1.1w-0+deb11u8` again. Since the build image moved to the Debian archive (#150) the bundle had silently regressed to the base image's `deb11u3`, missing 17 CVE fixes in ASN.1 / CMS / PKCS#12 parsing; the package is now fetched from snapshot.debian.org with a pinned sha256 per architecture.
