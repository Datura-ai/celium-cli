### Changed
- `lium mine` pulls the validator's preflight image in the background while the node starts (from step 3, "Checking prerequisites") and the validation step's spinner names the check that is running (GPU Configuration, matrix work-proof, VerifyX); when the image ends without a verdict the error carries its exit code and the last stderr lines instead of a bare JSON parse error. (DAH-2960)
