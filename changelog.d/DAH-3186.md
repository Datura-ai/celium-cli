### Security
- `paramiko` 4.0.0 → 5.0.0 (GHSA-r374-rxx8-8654: RSA signatures with SHA-1 are gone; SHA-1 key exchange and GSSAPI with them — the CLI only ever spoke SHA-2 to pod sshd) and `cryptography` 48.0.1 → 50.0.1 (GHSA-g6cj-pr64-35w5). The `cryptography<49` constraint in `pyproject.toml` and the matching Dependabot ignore are lifted: the `lium-darwin-amd64` job now builds cryptography from source with OpenSSL linked statically (`OPENSSL_STATIC=1`, Homebrew `openssl@3`), and both macOS smoke tests fail if `_rust.abi3.so` references a shared `libssl`/`libcrypto`. `e2e/requirements.txt`: pytest 8.4.2 → 9.0.3 (GHSA-6w46-j5rx-g56g).
